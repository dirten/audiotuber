- Read in MP3 file(s)
- Get filename
- Get ID3 tags from MP3
- Prompt for tags if no ID3
- Optionally specify background image
- Generate static image with imagemagick/graphicsmagick/gd
- encode as video with ffmpeg
- optionally upload

# ffmpeg cli
ffmpeg -loop 1 -framerate 2 -i input.png -i audio.m4a -c:v libx264 -preset medium -tune stillimage -crf 18 -c:a copy -shortest -pix_fmt yuv420p output.mkv

# album covert art
https://trac.ffmpeg.org/wiki/Encode/YouTube
